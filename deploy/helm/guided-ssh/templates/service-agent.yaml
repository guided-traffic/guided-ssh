{{- if and .Values.agent.enabled .Values.agent.service.enabled }}
# Separate service for the agent API (mTLS): agents outside the cluster
# need TLS passthrough — hence no ingress, but usually a LoadBalancer instead.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "guided-ssh.fullname" . }}-agent
  labels:
    {{- include "guided-ssh.labels" . | nindent 4 }}
  {{- with .Values.agent.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.agent.service.type }}
  ports:
    - name: agent
      port: {{ .Values.agent.service.port }}
      targetPort: agent
      protocol: TCP
  selector:
    {{- include "guided-ssh.selectorLabels" . | nindent 4 }}
{{- end }}
