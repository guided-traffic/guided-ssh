# Configuration for chart-testing (ct) — Helm chart linting in CI.
chart-dirs:
  - deploy/helm
target-branch: main
# Maintainers are an org, not GitHub users — validation would fail.
validate-maintainers: false
# Release happens via git tag, not via a chart version bump.
check-version-increment: false
