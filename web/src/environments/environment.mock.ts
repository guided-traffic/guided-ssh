/**
 * Mock environment (plain `ng serve`, wired via fileReplacements in
 * angular.json): the UI runs entirely without a backend — the mock
 * interceptor serves fixture data covering every UI variant, for
 * assessing the design at runtime.
 */
export const environment = {
  mockApi: true,
};
